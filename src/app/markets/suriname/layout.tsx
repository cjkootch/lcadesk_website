import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Suriname — Local Content Compliance",
  description:
    "LCA Desk for Suriname — prepare for local content compliance as Suriname's deepwater oil and gas sector develops with TotalEnergies and APA Corporation.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
